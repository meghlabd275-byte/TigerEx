[package]
name = "tigerex-auth"
version = "1.0.0"
edition = "2021"
description = "TigerEx Auth Service"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
ring = "0.16"
base64 = "0.21"

[features]
default = []